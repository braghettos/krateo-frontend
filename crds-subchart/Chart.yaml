apiVersion: v2
name: krateo-frontend-crd
description: Krateo PlatformOps Frontend CRDs

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# Versioned with the SAME release tag as the main chart (krateo-frontend): the CHART_VERSION
# placeholder is substituted to the git tag by release-oci.yaml, so a single chart-repo tag
# publishes krateo-frontend and krateo-frontend-crd at the same version. The installer therefore
# pins both at one version. (Was independently pinned at 1.0.25.)
version: CHART_VERSION


home: https://krateo.io
icon: https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg
keywords:
  - krateo
  - platformops
  - frontend
  - crd
sources:
  - https://github.com/braghettos/frontend
