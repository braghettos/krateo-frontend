# Notifications bell
# Target: the events SSE stream as a header bell + drawer.
# Expected behavior: a bell badge; clicking opens a drawer listing notifications.
kind: Notifications
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: example-notifications
  namespace: krateo-system
spec:
  widgetData: {}
